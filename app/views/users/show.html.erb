<h1>プロフィール</h1>

<h3>名前</h3>
<%= @user.name %>

<h3>年齢</h3>
<%= @user.age %>

<h3>アバター</h3>
<p>
  <% if @user.avatar.attached? %>
    <%= image_tag @user.avatar %>
    <% else %>
    <%"leave it blank if you don't want to change it"%>
  <% end %>
</p>

<h3>メールアドレス</h3>
<%= @user.email %>

<%= link_to 'Edit', edit_user_registration_path(@users) %> |
<%= link_to 'Back', users_show_path %>

